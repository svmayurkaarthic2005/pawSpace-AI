/**
 * Unified swipeable onboarding screen.
 * Users can swipe left/right to navigate between onboarding pages.
 * 
 * Previous multi-screen flow (OnboardingWelcome → OnboardingProfile → OnboardingDiscover)
 * has been consolidated into a single swipeable component for better UX.
 */
export { default } from './SwipeableOnboarding';
